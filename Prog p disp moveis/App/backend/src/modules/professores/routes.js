# Professores module configuration
# Routes para gerenciar professores

import { Router } from 'express';

const router = Router();

// TODO: Implementar endpoints de professores
// GET /api/professores - Listar
// GET /api/professores/:id - Buscar por ID
// POST /api/professores - Criar (admin)
// PUT /api/professores/:id - Atualizar (admin)
// DELETE /api/professores/:id - Deletar (admin)

export default router;
